# AWS RDS PostgreSQL Connection (defaults hardcoded in serverless.yml and rds.tf)
# Only override DB_HOST after running terraform apply
DB_HOST=your-rds-endpoint.eu-north-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=junction2025
DB_USER=postgres
DB_PASSWORD=Junction2025PostgresPass

# CORS
ALLOWED_ORIGINS=*
